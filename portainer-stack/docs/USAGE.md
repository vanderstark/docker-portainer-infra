# Usage

- Tambah Environment: UI → Environments → Add Environment
- Edge Agent: Copy EDGE_ID & EDGE_KEY ke `.env` → `docker compose up -d`
- Update Stack: `docker compose pull && docker compose up -d`